cmake_minimum_required(VERSION 2.8)
project(zigzag)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(path path.cpp outliers.hpp outliers.cpp)
target_link_libraries(path ${OpenCV_LIBS})
